<%= form_for(@bullying_type) do |f| %>
  <% if @bullying_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bullying_type.errors.count, "error") %> prohibited this bullying_type from being saved:</h2>

      <ul>
      <% @bullying_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
